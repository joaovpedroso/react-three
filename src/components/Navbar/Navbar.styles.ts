import styled, {} from "styled-components";

const NavList = styled.ul`
    list-style: none;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    padding: 0;
`;

export {NavList};